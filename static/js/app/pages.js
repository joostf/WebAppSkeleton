(function (window, document) {
	WEBAPP.pages = {
		about: function () {
			WEBAPP.states.handleState();
		},

		breakdown: function () {
			WEBAPP.states.handleState();
		},

		resources: function () {
			WEBAPP.states.handleState();
		}
	};
})(window, document);